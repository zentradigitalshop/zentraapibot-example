aiogram==3.30.0
httpx==0.27.2
python-dotenv==1.0.1
aiohttp==3.10.11

# PostgreSQL (Supabase). [binary] ships prebuilt wheels — no compiler needed
# on the VPS. Pinned with wheel coverage in mind: 3.2.13 covers Python 3.11
# through 3.13, where some earlier patch releases only shipped wheels for
# older interpreters.
psycopg[binary]==3.2.13
psycopg-pool==3.2.8
